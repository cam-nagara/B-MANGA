# 照合レポート 2026-07-03（設計意図 × 実態スナップショット）

- 作成日: 2026-07-03（仕様定点観測一式の③。履歴資料のため上書き禁止）
- 設計意図: `B-MANGA_設計意図.md` 0.14節 / `B-MANGA-Line_設計意図.md`（いずれも 2026-07-03 更新）
- 実態: `spec-snapshot-2026-07-03-bmanga-render-line.md`（コミット 13425e1 時点）
- 修正計画書: `spec_audit_fixes_plan_2026-07-03.md`

## 総評

2026-06-27〜07-03 のユーザー指示（B-MANGA Line 中心、約80項目）に対し、実装は139コミットで追随しており、**静的に確認できた範囲の大半（約8割）は達成**。未達・部分は少数だが、「交差線の作成方式選択UIの残存」などユーザー発言と齟齬の疑いがあるものが含まれる。静的判定できない挙動系が10項目前後残っており、実機での微細挙動テストが必要。

## A. 未達・部分達成（コード修正候補）

| # | 項目 | 設計意図 | 実態 | 重要度 |
|---|---|---|---|---|
| A-1 | 交差線「作成方式」選択UI | 「ライン素材（高速）」のみが正。方式の選択制は依頼していない（2026-07-02 14:27 の強い訂正） | SHELL（既定）/BOOLEAN/SDF の3択ドロップダウンが panels.py:261 に露出 | 高（ユーザー判断要） |
| A-2 | シードの一元化 | ズラし量の右隣の「シード」でそのオブジェクトの**全**「乱れ」を一元化。複製時にシード不変 | shape_seed は実装・配置済みだが、線種図形用「乱れシード」(line_shape_seed) が別に残存。複製時のシード維持は未検証 | 中 |
| A-3 | 効果線とウニフラ・白抜き線の完全一元化 | 同じ設定が二重管理にならない一元化 | UI部品は共通化済みだが保存フィールドは別名のまま対応表で吸収（部分統合）。片側だけ修正されるリスクが残る | 中（構造課題） |
| A-4 | ウニフラ始点乱れの初期値オン | 初期値オン（2026-06-28） | プロパティ default=False。ウニフラ選択時に defaults 適用で True になる経路あり — 実機で新規作成時にオンになるか確認要 | 低 |
| A-5 | AOV版「ラインのみ表示」コードの残存 | AOV方式は重いため不採用（2026-07-01） | viewport_aov.py(181行) が残存（現在は無効化経路のみ使用）。誤って再有効化される導線はないが整理候補 | 低 |

## B. 静的判定不能 — 実機確認が必要（微細挙動テスト候補）

| # | 項目 | 備考 |
|---|---|---|
| B-1 | 枠線カット後のレイヤーリスト順・階層維持 | 実装は `align_coma_order=True` で静的には「数値順整列」に見える。ユーザーは 2026-06-28 20:10 に一覧順維持を確認済みのため、回帰していないか実機確認 |
| B-2 | 枠線カット直後の自動再採番 | 完全自動で毎回実行されるか |
| B-3 | 円柱のフチのような内部線への中間頂点調整（B-MANGA Line） | 2026-07-02 17:07 依頼。実装済みか静的に確認できず。**最後の依頼群のため未実装の可能性あり** |
| B-4 | 交差線の品質（出るべき場所に出る/余計な場所に出ない/円錐×キューブで1本/線幅・グラフが効く） | 423ad18 等で修正済みの見込みだが実機確認要 |
| B-5 | ハンドルが対象より一回り大きい | 外側のみ化(72fb72e)は済み。サイズは実機確認 |
| B-6 | ページ選択で真っ白にならない | 0935efa で修正済みの見込み |
| B-7 | 魚眼モードのグレー帯がレンダリング領域に連動 | 静的に該当コード未発見 — **未実装の可能性あり** |
| B-8 | 編集中ページの用紙ガイドが太くならない | 静的に判定不能 |
| B-9 | ツールプリセット切替（なめらか自由形状で固まらない）・カーソル残留 | 静的に判定不能 |
| B-10 | コマファイルからのリンク一括上書き/補正（B-MANGA Line） | オペレータは存在（refresh_linked / apply_active_to_linked）。挙動未検証 |
| B-11 | 矩形丸角100%で長細くしても直線が生じない | 実装ありの模様、形状は実機確認 |
| B-12 | Tokyo0004 での「レンダリング範囲内選択→3種オン→ラインを適用」の所要時間 | 2026-07-01 22:38 に30分超の回帰報告あり。07-02 大規模監査では1秒前後の記録だが、同条件での再現確認要 |

## C. 要ユーザー判断（次回関連作業時に選択肢を提示）

| # | 論点 | 選択肢 |
|---|---|---|
| C-1 | 交差線「作成方式」ドロップダウン（A-1） | (a) UIから削除して高速方式に固定【設計意図に忠実・推奨】 (b) 開発用として残す |
| C-2 | アウトラインのチェックボックス初期値 | 現行オン。(a) オンのまま【ライン適用が直感的・推奨】 (b) 全オフ指示に合わせてオフ |
| C-3 | 板ポリ除外の初期値 | 現行オフ（「全チェックボックスオフ」14:09 指示に準拠）。(a) このまま (b) 13:55 の「除外オン」を生かしてオン |
| C-4 | B-MANGA Render タブ | 2026-06-28 実装: 常時表示+コマファイル外は案内表示（暫定観測）。この挙動で確定してよいか |

## D. 達成確認済みの主要項目（抜粋）

- B-MANGA Line: 線幅0.3mm初期値・印刷mm基準、線幅基準距離2m、検出角度60度、作成範囲制限オン(10m)、内部線/交差線初期オフ、「線幅の均一化（オブジェクト単位/頂点単位）」の名称と配置、3種別の色/オフセット/距離非表示、ラインのみ表示=マテリアル差し替え+オフセット1.0維持、AOV自動組込・自動補修、プリセット管理、レンダリング範囲内選択（魚眼対応）、保存時自動スムーズ保護、中間頂点（乱れ/グラフ/3種別/検出角度別）、生成済みラインのみ更新の軽量化、透明裏面対策、指定辺モード
- 本体: パターンカーブ改名・スタンプ/リボン・角度3択・生成形状・入り抜きグラフ4数値連動・線幅/不透明度/色の同時適用、効果線ベースパス編集、フキダシ/NURBS統合（Phase0-3完了）、ダイアログ多列化・同幅・幅自動変更、塗り輪郭ぼかし軸3択、矩形の丸角/角半径、小山高50%と乱れ、コマ結合（ダイアログ2択）、基本枠コマ復活、Shiftスナップ、Ctrl+Shift+D、読む方向対応の前/次ボタン、コマファイル3トグル分離、右クリックメニュー整理
- Render: タブ常時表示化（2026-06-28 df64ecf）

## 記録

- 照合最終実施日: 2026-07-03（次回リマインド基準日）
- 未達差分は AGENT_INBOX.md「仕様定点観測 2026-07-03 未達差分」項目で追跡。コード修正は `spec_audit_fixes_plan_2026-07-03.md` に集約
